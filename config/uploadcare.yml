defaults: &defaults
  public_key: "<%= ENV['PUBLIC_KEY'] %>"
  private_key: "<%= ENV['PRIVATE_KEY'] %>"

development:
  <<: *defaults

test:
  <<: *defaults
  autostore: false

production:
  <<: *defaults
